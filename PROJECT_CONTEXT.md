# MANDATUM 프로젝트 기준 문서

이 문서는 이전 `웹사이트 제작 요소` 대화에서 사용자와 문답하며 확정한 내용을 현재 작업 세션에 옮긴 것이다. 이후 구현·수정 시에는 중간 시안보다 아래의 최신 합의를 우선한다. 사용자가 새로 변경하는 내용이 생기면 이 문서도 함께 갱신한다.

## 1. 사이트의 목적과 정체성

- 캐릭터챗 세계관을 처음 보는 사람이 세계 구조와 인물, 펄스, 사회 설정을 이해하고 단편 웹툰까지 감상하는 공식 세계관 사이트다.
- 성격은 `현대적인 게임 IP 공식 홈페이지 + 캐릭터/웹툰 공식 사이트 + 세계관 내부 데이터베이스`의 혼합형이다.
- 일반 UI는 간결하게 유지하고, WORLD와 ARCHIVE에서만 게임 UI·데이터베이스 감각을 강화한다.
- 제공되지 않은 설정, 인물, 사건, 날짜, 통계는 임의로 창작하지 않는다. 빈 콘텐츠는 추후 추가 가능한 데이터 구조와 UI만 둔다.
- PC 중심으로 설계하되 태블릿·모바일에서도 모든 핵심 기능을 사용할 수 있어야 한다. Hover 전용 기능은 모바일에서 Tap 또는 길게 누르기로 대체한다.
- 모바일에서는 공통 상단 UI의 간격과 글자 크기, 필터의 가로 스크롤, 상세 패널의 안전 영역을 별도로 최적화한다. WORLD는 기기 폭·메모리·CPU를 기준으로 렌더 해상도, 그림자, 후처리 품질을 자동 조절해 WebGL 컨텍스트 손실을 줄인다.

## 2. 디자인 언어

- 배경: `#0B1016`
- 보조 배경: `#121A23`
- Surface: `#18232F`
- 기본 글자: `#E9EEF3`
- 보조 글자: `#8996A3`
- 기본 Accent: `#4D8DFF`
- 밝은 Accent: `#8AB8FF`
- Border: `#293644`
- 위험·사건 Accent: `#E05A63` — 위험, 재난, 사망, 에이저, 경고 등 의미가 있을 때만 사용한다.
- MANDATUM 로고는 얇고 고전적인 Serif Display 계열(Cinzel 우선)과 넓은 자간을 쓴다. 본문과 데이터 UI는 가독성 높은 Sans-serif를 유지한다.
- 얇은 테두리, 충분한 여백, 큰 이미지, 명확한 제목, 작은 영문 메타데이터를 사용한다.
- 과도한 Gradient, Glow, Glassmorphism, 그림자, Neon, 둥근 카드, Pill 버튼, Emoji 아이콘을 피한다.
- 페이지 이동은 `180~250ms` 정도의 짧은 Fade 위주다. 과도한 Slide, Spring, Bounce를 사용하지 않는다.

## 3. 전체 정보 구조

```text
HOME
├─ WORLD
│  └─ 중앙섬 / 북쪽 섬 / 남쪽 섬 / 동쪽 섬 / 서쪽 섬
├─ CHARACTERS
├─ WEBTOON
└─ ARCHIVE
   ├─ WORLD
   ├─ PULSE
   ├─ HISTORY
   ├─ INCIDENTS
   ├─ CREATURES
   ├─ SOCIETY
   └─ RELATIONSHIPS
```

- 일반 서브페이지의 BACK은 HOME으로 돌아간다.
- WORLD에서 섬이 확대된 상태라면 첫 BACK은 전체 지도, 다음 BACK은 HOME이다.
- CHARACTERS에서 상세 패널이 열려 있다면 첫 BACK은 상세 닫기, 다음 BACK은 HOME이다.
- ARCHIVE 문서에서는 `문서 → ARCHIVE 목록 → HOME` 순서로 돌아간다.

## 4. 진입 흐름과 인트로

최신 확정 흐름은 다음과 같다.

```text
사이트 최초 진입
→ MANDATUM 타이틀 + START
→ START 클릭
→ AETHER SYSTEM 기동 인트로 + 기계식 사운드
→ HOME
```

- START는 약 `220×62px`의 각진 박스 버튼이며 시스템 기동 트리거다.
- START 클릭 이전에 오디오를 자동 재생하지 않는다. AudioContext는 클릭에서 생성하거나 재개한다.
- 인트로는 외부 이미지나 웹툰 컷 몽타주에 의존하지 않는다. HTML/CSS/inline SVG 기반으로 AETHER CORE, 회전 링, 진행률, 조직 연결, WORLD NETWORK를 표현한다.
- 일반 모션 기준 인트로 전체 길이는 약 8초로 두어 각 기동 단계를 충분히 보여준다. `prefers-reduced-motion` 사용자의 단축 흐름은 유지한다.
- 사운드는 거대한 에테르 기반 기계가 기동하는 인상이다. 밝은 게임 UI음이나 레이저음보다 낮은 Impact, Relay Tick, Hum, Core Lock, System Online 확인음을 쓴다.
- 인트로가 완전히 끝나 HOME이 표시되는 순간에만 `sessionStorage`에 입장 완료를 기록한다.
- 같은 브라우저 탭에서 서브페이지를 다녀오면 START와 인트로를 반복하지 않고 즉시 HOME으로 돌아간다.
- 개발 확인용 `?intro=1` 강제 재생 경로는 허용한다.
- 사이트 BGM은 느린 미니멀 솔로 피아노 실제 음원 `mandatum-cinematic-bgm.mp3`를 사용한다. 짧고 성긴 피아노 동기를 반복하며 큰 고조나 추가 악기층을 거의 두지 않아, 이전보다 단조롭고 배경에 머무는 피폐한 히어로물 분위기를 유지한다. 데이터 안내 확인 직후 로딩·재생을 시작하며, 타이틀 임팩트를 가리지 않도록 인트로 진입 시 약 5.2초에 걸쳐 서서히 올라온 뒤 모든 페이지에서 반복 재생한다. 최초 기본 BGM 볼륨은 10%이며, 우측 상단의 고정 컨트롤에서 음소거와 세부 볼륨 조절이 가능하다. 사용자가 직접 바꾼 음소거·볼륨 설정은 브라우저에 기억한다. 이전 합성 BGM의 설정과 충돌하지 않도록 별도 저장 키를 사용한다. MANDATUM 로고와 START 버튼의 등장음은 합성 오실레이터를 쓰지 않고 프로젝트에 포함한 실제 시네마틱 음원 `title-logo-reveal.mp3`, `title-start-impact.mp3`를 사용한다. 두 파일은 Pixabay Content License로 제공되는 Logo Reveal (Bass Drop + Echo)와 Impact Whoosh 원본이며, 화면 등장 시점에 맞춰 재생하고 긴 잔향은 페이드아웃한다. 파일 로딩 실패 시 값싼 합성음으로 대체하지 않고 조용히 생략한다. 두 요소가 나타난 뒤 간격이 부드럽게 벌어질 때는 좌우로 이동하는 짧은 슬라이드 스윕음을 더하고, 로고 아래에 `-히어로 이야기-` 부제가 나타난다. 인트로의 기계음과 WORLD의 서버룸 효과음도 화면 연출용 효과음으로 유지한다. 사이트 전역의 링크·버튼·입력 요소·선택 가능한 목록·관계도 노드·캔버스에는 이벤트 위임 방식의 UI 사운드를 적용한다. 호버음은 멜로디가 없는 고역 데이터 틱과 릴레이 클릭으로, 선택음은 짧게 게이트된 노이즈 패킷과 비화성 디코더 펄스로 구성해 해커 터미널 느낌을 낸다. 터치 포인터의 호버음과 동일 요소 내부 이동에 따른 중복 재생은 제외한다.

## 5. HOME

- 상단에 MANDATUM 로고, 그 아래 `WORLD | CHARACTERS | WEBTOON | ARCHIVE` 네 개의 대형 비주얼 패널을 둔다.
- HOME의 대표색은 하위 화면까지 이어진다. WORLD `#2DD4FF`, CHARACTERS `#B77AFF`, WEBTOON `#FFB45E`, ARCHIVE `#62E6A7`을 각각 제목 밑선, 상단 채널선, 모서리 HUD, 뒤로가기 Hover, 주요 패널·선택 상태의 테두리와 약한 배경 Glow에 사용한다.
- Desktop에서는 가로 4분할, Tablet은 필요하면 2×2, Mobile은 세로 4개다.
- Desktop 컨테이너 기준은 최대 폭 약 `1480px`, 좌우 여백 약 `40px`, 높이 `clamp(520px, 68vh, 720px)`다.
- 기본 네 패널은 항상 DOM에 존재하며 자동 Fade-out, 반복 opacity 애니메이션, 자동 Unmount가 없어야 한다.
- Hover한 패널은 약 38%까지 넓어지고 나머지는 각각 약 20.7%로 좁아진다. 작은 화면에서도 제목과 클릭 영역이 무너지지 않는 범위에서 확장감을 높인다. 내부 콘텐츠까지 고무처럼 늘어나는 FLIP 보간은 사용하지 않고, 이전과 같은 `flex-basis` 경계 이동에 필요한 속성만 한정해 자연스럽게 전환한다.
- 패널별 Accent:
  - WORLD: Cyan `#2DD4FF`
  - CHARACTERS: Violet `#B77AFF`
  - WEBTOON: Amber `#FFB45E`
  - ARCHIVE: Mint `#62E6A7`
- Accent는 카드 전체를 칠하지 않고 패널별 Border, 상단 Line, 설명 Text와 약한 외곽 Glow에만 사용한다. Hover 시 해당 색의 선명도와 Glow가 조금 강해진다.

### START 이후 공통 배경

- `https://igx.kr/r/t8/0/4`, `/0/5`, `/0/6` 세 이미지를 START 이후 HOME과 CHARACTERS, WEBTOON, ARCHIVE의 공통 뒷배경으로 사용한다. `/0/7`은 공통 뒷배경에서 제외한다.
- 세 이미지는 약 12초 간격으로 천천히 교차 전환하며, 콘텐츠 가독성을 위해 어두운 색조와 가장자리 음영을 겹친다.
- HOME에서는 이미지 자체의 밝기와 중앙부 투과도를 다른 서브페이지보다 높여 배경이 지나치게 어둡지 않게 한다.
- START 화면은 첫 프레임부터 완전히 불투명한 검정 배경을 표시하고, 그 위에서 MANDATUM → `-히어로 이야기-` → START 순서로 등장한다. 부제는 로고 직후부터 알아볼 수 있게 표시하고 지나치게 늦게 나타나지 않는다. 최초 라우트 컨테이너와 START 배경에는 진입 Fade를 적용하지 않아 공통 배경 이미지가 먼저 보였다가 사라지는 현상이 없어야 한다. Intro도 기존 불투명 배경을 유지한다.
- 3D 전용 배경을 별도로 만들 예정인 `/world` 경로에는 이 공통 이미지 배경을 적용하지 않는다.
- 공통 배경 이미지도 사이트 접속 즉시 백그라운드 선로딩 대상에 포함한다.
- HOME의 WORLD 패널은 `/0/4`, `/0/5`, `/0/6` 세 이미지 중 하나를 CHARACTERS·WEBTOON과 같은 Scan/Wipe 방식으로 무작위 표시한다.
- WORLD 패널의 무작위 선택은 HOME 뒷배경 순환과 독립적으로 동작하며, 같은 이미지가 동시에 표시되는 것도 허용한다.
- HOME의 ARCHIVE 패널은 `/0/7` 이미지를 고정적으로 표시한다.
- 사이트를 새로 접속하거나 새로고침할 때마다 대용량 이미지와 모바일 데이터 사용량을 알리는 고지 화면을 먼저 표시한다. 확인 전에는 App을 렌더링하지 않아 이미지 요청이 시작되지 않으며, 확인 버튼을 누르는 순간 전역 이미지 사전 로딩과 사이트 진입을 함께 시작한다. 확인 상태는 저장하지 않으며 같은 접속 안의 페이지 이동 중에는 다시 표시하지 않는다.
- 확인 이후의 전역 이미지 사전 로딩은 첫 화면 필수 이미지부터 최대 2개씩 처리하고, 패널 이미지와 상세·웹툰 이미지를 충분한 간격으로 단계적으로 이어 받아 대용량 요청이 첫 화면과 애니메이션을 막지 않게 한다.
- 확인 버튼을 누르면 가능한 브라우저에서 영구 저장 권한을 요청하고, Service Worker의 `mandatum-image-cache`에 모든 이미지 응답을 Cache First 방식으로 보관한다. 한 번 내려받은 이미지는 사이트 데이터가 삭제되거나 브라우저 저장 공간 정책으로 제거되기 전까지 네트워크에서 다시 받지 않는다.
- HOME의 무작위 패널은 현재 이미지와 다음 이미지를 별도 고우선순위로 미리 받아 `decode()`까지 끝낸 뒤 최대 3장의 제한된 메모리 캐시에 보관한다. 파일 자체는 영구 캐시에 제한 없이 누적하고, 디코딩된 원본만 제한해 메모리 과부하를 막는다. 백그라운드 선로딩과 화면용 디코딩은 같은 요청을 공유해 중복 다운로드를 피하고, 전환 시 준비된 레이어만 교체한다.
- 모바일 카드의 길게 누르기는 브라우저의 불안정한 가상 Hover에 맡기지 않고 Pointer 입력을 약 280ms 감지해 Scan을 시작한다. 길게 누른 뒤에는 링크 이동을 한 번 막고, 짧게 Tap하면 기존처럼 해당 페이지로 이동한다.
- 터치 기기나 메모리가 작은 기기에서는 고해상도 레이어의 실시간 Clip-path 변형 대신 짧은 합성 Crossfade와 기존 대각선 Scan Line을 조합해 같은 연출을 더 안정적으로 표시한다.
- Touch의 첫 Tap은 미리보기에 소비하지 않고 해당 페이지로 이동한다.
- START와 인트로가 끝나 처음 HOME에 도착하면 4단계 내비게이션 튜토리얼을 한 번 표시한다. WORLD → CHARACTERS → WEBTOON → ARCHIVE 순서로 실제 패널을 강조하고 각 페이지에서 가능한 조작과 열람 내용을 설명한다. 이전·다음·건너뛰기·완료와 좌우 방향키·Enter·Esc 조작을 지원하며, 서브페이지에서 HOME으로 돌아올 때는 반복하지 않는다.
- HOME 상단의 MANDATUM 로고는 기존 비율을 유지한 채 높이만 약 10% 키워 가독성을 조금 높인다.

### HOME의 CHARACTERS 패널 이미지 — 최신 확정 상태 머신

```text
HOME 진입
→ 랜덤 Character A(캐릭터 ID 1~21, 이미지 14 또는 15)를 미리 선택·로드
→ A를 흑백으로 표시

Hover
→ URL을 바꾸지 않음
→ 동일한 A의 흑백 레이어 위에 컬러 A를 Scan Reveal
→ 컬러 A

Mouse Leave
→ 다음 랜덤 Character B를 미리 준비
→ 컬러 A 위로 흑백 B를 Scan Wipe
→ 흑백 B
```

- 상태는 `gray → revealingColor → color → revealingNext`로 명확하게 관리한다.
- Scan Line과 `clip-path` Reveal 경계는 항상 일치해야 한다.
- Scan/Wipe 경계는 수직선이 아니라 약 14도 기울어진 굵은 대각선 광선이며, 색상은 각 패널의 Accent를 따른다. 광선은 패널 크기의 대각선 그라데이션 레이어 전체를 좌우 바깥으로 이동시켜 세로로 긴 패널에서도 회전된 선의 끝부분이 화면에 남지 않게 한다. Reveal 완료 시 컬러 레이어의 `clip-path`를 `none`으로 해제해 경계선 잔상을 없앤다.
- 이미지 로드 전에 Scan을 시작하지 않는다.
- 패널 강조 Transform과 이미지 Wipe를 분리한다. 이미지 레이어는 항상 `position:absolute; inset:0; width:100%; height:100%; object-fit:cover; object-position:center top`을 유지한다.
- 이미지 `width`, `scaleX`, `src`의 순간 교체로 Wipe를 흉내 내지 않는다.

## 6. WORLD

- 세계는 총 다섯 개의 서로 멀리 떨어진 거대한 공중섬으로 구성된다.
- 중앙섬은 별도 고유명이 없으며 단순히 `중앙섬`이다.
- 조직명과 섬 이름은 구분한다. `EASTER ISLAND`처럼 쓰지 않는다.

| 위치 | 표시 이름 | 기후·도시 | 히어로 조직 |
|---|---|---|---|
| 중앙 | 중앙섬 | 현대 인프라가 완전히 갖춰진 대도시 | LADER, PACTUM |
| 북 | 북쪽 섬 | 한랭 기후, 지방 도시와 시골의 중간 | NIVALI |
| 남 | 남쪽 섬 | 온난 기후, 지방 도시와 시골의 중간 | SOLARIA |
| 동 | 동쪽 섬 | 현대 한국형 사계절, 지방 도시와 시골의 중간 | EASTER |
| 서 | 서쪽 섬 | 현대 한국형 사계절, 지방 도시와 시골의 중간 | ORIA |

- 스타일은 게임 월드맵형 3D 미니어처 디오라마다. Google Maps 같은 현실 지도나 평면 대륙처럼 보이면 안 된다.
- 다섯 섬 사이에는 확실한 빈 공간이 있어야 하며 현재보다 2~3배 멀게 느껴져야 한다.
- 중앙섬과 북·남·동·서 외곽 섬 사이에는 각각 하나의 기계식 공중교를 둔다. 총 4개이며 금속 데크, 트러스, 케이블, 행어, 교각형 앵커와 절제된 에테르 유도선으로 구성한다.
- 섬 Hover는 간략 정보, Click은 카메라가 실제 해당 섬으로 이동·확대한다.
- 확대 후에는 기후, 조직, 본부 위치, 소속 히어로 정도만 보여준다. 미제공 랜드마크를 임의로 창작하지 않는다.
- 기본 카메라는 약간 기울어진 3D 시점이며, 제한된 범위에서 약간 회전할 수 있다. 섬 선택·확대·모바일 안정성이 회전보다 우선이다.
- WORLD 진입 시 다운로드 음원 없이 Web Audio로 합성한 낮은 서버룸 기계음과 `삐빅-삑` 상태 비프가 작게 시작된다. 비프는 불규칙한 간격으로 반복되며 WORLD를 벗어나면 모든 관련 음원과 타이머를 정리한다.

### 공중섬의 핵심 설정

- 자연적으로 떠 있는 판타지 섬이 아니다.
- 상부는 도시·산·평야·도로·숲이 있는 실제 땅이고, 하부 암반에는 섬 전체를 띄우고 이동시키는 초대형 기계식 에테르 부유·추진 엔진이 박혀 있다.
- 시각 조합은 `Floating Island + Mega Structure + Aether Engine`이다.
- 하부에는 원형·다각형 추진기관, 코어, 안정화 장치, 금속 프레임, 장갑판, 배관, 플랫폼 등을 절제해 사용한다.
- 에테르는 차가운 Blue 계열 기술 동력으로 표현한다. 마법진이나 로켓 화염이 아니라 엔진 내부의 은은한 Core, 얇은 에너지 라인, 아래로 흐르는 에테르와 공기 왜곡으로 보인다.
- 하부 구름층은 외부 CDN 텍스처에 의존하지 않고, 셰이더와 인스턴싱으로 생성한 부드러운 절차적 구름을 사용한다. 따라서 외부 이미지 CORS·Service Worker 오류가 WORLD 렌더링 전체를 중단시키지 않아야 한다.

## 7. CHARACTERS

- 모든 캐릭터를 카드 Grid로 보여주고 상단에서 조직별 필터를 제공한다.
- 상세 정보에는 사용자가 제공한 `이름 / 성별 / 나이 / 등급 / 소속 / 펄스 / 성격 / 특징`을 생략 없이 표시한다.
- Desktop은 Hover로 요약, Click으로 상세 패널을 연다. Mobile은 첫 Tap으로 즉시 상세를 연다.
- 캐릭터 데이터는 별도 데이터 파일로 유지하고, 이미지 URL용 ID는 명시적인 매핑 테이블로 관리한다. 이름의 배열 순서나 문자열에서 ID를 추측하지 않는다.
- 이미지 URL: `https://igx.kr/r/t8/{imageId}/{imageNumber}`
- 상세 패널은 선택한 캐릭터의 고정 ID와 `imageNumber=0`을 사용한다. 이미지는 원본 약 `3192×2275` 비율의 가로형 영역에 `contain`으로 표시해 잘림과 확대 합성으로 인한 흐림을 막는다.
- CHARACTERS, WORLD, RELATIONSHIPS의 캐릭터 상세 이미지는 모두 같은 `3192×2275` 비율을 사용한다. 상세 이미지를 다시 선택하면 원본 비율의 전체 화면 확대 보기가 열리며, 배경·닫기 버튼·Esc로 닫을 수 있다.
- 세 상세 화면의 이미지 바로 아래에는 `이미지를 눌러 크게 볼 수 있어요`라는 안내를 푸른 홀로그램 말풍선 형태로 공통 표시한다. 이미지 쪽을 가리키는 꼬리, 확대 아이콘, `TIP` 라벨, 밝은 테두리와 약한 Glow를 사용한다.
- 상세 이미지 로딩 실패 시 기존 Placeholder를 표시한다.
- 금하나·한유나·소라는 인물 중심이 더 자연스럽게 보이도록 공통 이미지 초점을 오른쪽으로 소폭 보정한다. 이 보정은 상세 패널, HOME 랜덤 이미지, WORLD의 히어로 목록에 동일하게 적용한다.
- 세이지의 15번 이미지에는 오른쪽 초점 보정을 별도로 적용하고, 금하나의 15번 이미지는 기존 공통 보정보다 조금 더 오른쪽으로 이동한다.
- 이번 이미지 시스템 적용 범위에는 Grid 카드의 전투 이미지 전환이 포함되지 않는다. 랜덤 14·15번 이미지는 HOME의 CHARACTERS 패널에서만 사용한다.
- CHARACTERS Grid의 모든 카드는 각 캐릭터의 코드 0 외부 이미지를 사용한다.
- CHARACTERS Grid 카드는 실제 대표 이미지의 약 `3192×2275` 비율로 통일한다. 카드의 Scale 기반 등장·재배치는 사용하지 않고 위치만 이동하며, 원본 디코딩이 완료되기 전의 거친 중간 프레임은 숨긴 뒤 한 번에 선명하게 표시한다.
- CHARACTERS Grid는 화면 근처 카드부터 순차적으로 고해상도 이미지를 디코딩하며, 한 번 화면에 나타난 카드는 이후 스크롤로 멀어져도 이미지 요소를 유지한다. 화면 전환·상세 패널 이동이 끝난 뒤에는 상위 요소의 합성 Transform을 제거해 이미지가 저해상도 레이어에 남지 않도록 한다.
- 상세보기로 선택된 캐릭터의 Grid 카드는 화면 최적화에 의해 이미지를 해제하지 않는다. 따라서 목록 카드와 상세 패널 두 위치에 같은 대표 이미지가 항상 함께 표시된다.
- CHARACTERS 상세 패널은 Desktop에서 화면 오른쪽에 고정되고 목록과 독립적으로 스크롤한다. 목록을 스크롤해도 상세 패널의 위치와 자체 스크롤은 변하지 않으며, 고정 헤더의 `상세보기 닫기`를 누르면 패널이 닫히고 Grid가 전체 너비를 다시 사용한다.
- 조직 필터는 별도의 `ORGANIZATION FILTER` 제어 패널로 표시한다. 버튼은 최소 48px 높이를 확보하고 활성 조직은 푸른 배경·밝은 테두리·약한 Glow로 강조하며, 현재 결과 수를 함께 표시한다.

### 최신 캐릭터 이미지 ID

| ID | 이름 | ID | 이름 | ID | 이름 |
|---:|---|---:|---|---:|---|
| 1 | 송서연 | 8 | 타비 | 15 | 스이 |
| 2 | 유해인 | 9 | 한지원 | 16 | 김지현 |
| 3 | 하시연 | 10 | 세이지 | 17 | 아이라 |
| 4 | 한유나 | 11 | 윤세하 | 18 | 소라 |
| 5 | 임하린 | 12 | 윤현아 | 19 | 금하나 |
| 6 | 채나현 | 13 | 이루하 | 20 | 오하루 |
| 7 | 채인아 | 14 | 세츠나 | 21 | 크리스 |

### 이미지 번호

- 0 평상시 대화
- 1 미소·웃음
- 2 장난·신남
- 3 부끄러움
- 4 놀람
- 5 삐짐
- 6 화남·분노·진지·결연
- 7 시무룩·우울
- 8 눈물·울음
- 9 의문·궁금
- 10 졸림·피곤
- 11 걱정·초조
- 12 경멸·혐오
- 13 치명상
- 14 근접전투
- 15 펄스 사용·에테르 컨트롤

## 8. WEBTOON

- 일반 웹툰 사이트형이다.
- 기본 흐름은 `작품/회차 목록 → 회차 선택 → 중앙 정렬 세로 스크롤 감상`이다.
- PC에서는 적절한 최대 폭, 모바일에서는 화면 폭을 사용한다.
- 감상 화면에서는 다른 SF 효과를 최소화하고 읽기에 집중한다.
- HOME WEBTOON 패널용 이미지는 `https://igx.kr/r/t8/28/0`부터 `/6`까지 총 7장이다.
- HOME의 WEBTOON 패널은 위 7장 중 무작위 흑백 이미지로 시작하고, Hover 시 스캔을 거쳐 컬러로 전환하며, 이탈하면 다음 무작위 이미지로 넘어간다.
- 실제 웹툰 원고는 `https://igx.kr/r/t8/27/0`부터 `/28`까지 총 29장이다.
- WEBTOON 감상 화면은 원고 29장을 0번부터 28번까지 빈틈 없이 한 장씩 아래로 이어 붙여 한 번의 세로 스크롤로 감상하게 한다.

## 9. ARCHIVE

- ARCHIVE 루트의 7개 분류는 이미지 카드가 아니라 번호·분류명·데이터베이스 섹터 표기로 이루어진 세로 목록을 사용한다. 분류용 이미지는 사용하거나 사전 로딩하지 않는다.
- ARCHIVE의 뒤로가기는 브라우저 방문 이력을 사용하지 않는다. 루트 목록에서는 HOME으로, 세부 문서에서는 ARCHIVE 루트 목록으로 고정 이동해 WORLD와 ARCHIVE 사이의 이력 반복을 막는다.

- WORLD: 다섯 섬, 기후, 중앙·외곽 인프라 차이를 정리한다.
- PULSE: 펄스 설정과 개별 펄스 도감 구조다.
- HISTORY: `최초의 에이저 아담 등장 → 아담이 인간으로 진화 → 펄스 유전자가 점차 퍼져 현대 사회에 이름`까지만 공개한다. 나머지는 미확인 기록으로 남긴다.
- INCIDENTS: 현재 내용은 비우고 나중에 데이터 항목을 추가하면 자동으로 UI가 생기게 한다.
- CREATURES: 이름, 이미지, 위험도만 표시하고 나중에 하나씩 추가한다.
- SOCIETY: 모든 인간이 잠재적 능력자인 불안정한 사회를 기준 설정으로 삼는다. 중앙·동·서·남·북의 5개 거대 공중섬과 그 아래의 에이저 밀집 구역, 특수 고등학교 재학 중 자격 시험에 합격해야 취득하는 히어로 면허, 펄스의 에테르 소모와 에테르 번, 0~5세대 분류 및 동일 세대 내 숙련 격차를 설명한다. 현재 이슈로 치안 불신과 반히어로 여론, 매년 수백 명 규모의 능력자 실종, 임효린 피살 생중계 사건, 지방섬 히어로 인력난, 공중섬 내 에이저 출몰을 기록한다. 에이저는 에테르를 받아들이지 못한 각성 실패자가 괴물이 된 존재이며, 이 사실은 세간에 비공개인 기밀 정보로 구분한다.
- RELATIONSHIPS: 참고 이미지처럼 카드 목록이 아닌 자유 배치형 인물 관계도를 사용한다. 중앙 RELATION CORE에서 7개 조직 허브로, 각 조직 허브에서 21명의 원형 인물 노드로 다시 갈라지는 2단 방사형 가지 구조다. 각 인물 노드와 선택 상세에는 캐릭터 데이터베이스·WORLD와 동일한 `코드/0` 대표 이미지와 인물별 위치 보정을 적용한다. 원형 이미지는 고해상도 원본을 사용하고 캔버스 이동과 노드 좌표를 정수 픽셀에 맞춰 흐려지는 합성 현상을 방지한다. 조직을 둘러싸는 타원·카드 경계는 사용하지 않는다. 관계망은 `2600×1900px` 대형 캔버스로 확장하고, 제한된 뷰포트 안에서 마우스·터치 드래그로 지도처럼 이동한다. 최초 진입과 `CENTER VIEW` 선택 시 중앙 코어가 화면 가운데 배치된다. 코어-조직과 조직-인물 선은 소속 계통이며 개인 관계가 아니다. 현재 제공된 관계 데이터만 별도의 밝은 Canvas 연결선·화살표·선 위의 관계 라벨로 표시하고, 조직을 넘는 개인 관계는 점선으로 구분한다. 인물 노드를 선택하면 해당 인물과 연결된 가지와 관계선이 강조되며, 캐릭터 데이터베이스와 동일한 기본 정보·펄스·성격·특징 및 개인 관계를 갖춘 상세 패널이 즉시 열린다. 관계를 선택하면 패널 안에서 상세 설명을 제공하고, 양방향으로 중복 입력된 동일 관계는 하나의 연결로 정리한다.
- ORGANIZATIONS와 LOCATIONS는 독립 카테고리로 두지 않고 WORLD에 흡수한다.

## 10. 구현 우선순위와 변경 원칙

1. 기능 안정성과 기존 동작 보존
2. 진입/뒤로가기 상태
3. HOME 안정성 및 인터랙션
4. CHARACTERS 실제 이미지 연결
5. WORLD 고도화
6. 실제 이미지·웹툰·아카이브 콘텐츠 보강

- 한 번의 수정에서 범위를 작게 잠근다. 관련 없는 App, Router, Intro, HOME 등을 `유지하기 위해` 재작성하지 않는다.
- 필요한 파일만 수정하고, 범위를 넓혀야 한다면 이유를 먼저 확인한다.
- 이미지 사전 로딩은 최초 대용량 미디어 고지의 확인 상태에만 연결하며, 확인 이후에는 Intro와 각 페이지 상태에 종속시키지 않는다.
- 사이트 진입 즉시 현재 코드가 알고 있는 로컬 이미지, 캐릭터 이미지, 웹툰 원고 이미지를 백그라운드에서 선로딩한다. 이 작업은 Intro와 화면 진입을 막지 않는다.
- 빈 Placeholder, 이미지 Pop-in, 중복 이벤트, 애니메이션 중 이미지 왜곡을 허용하지 않는다.

## 11. 현재 ZIP 코드와의 확인된 차이

이 항목은 컨셉 변경이 아니라 다음 구현 작업에서 확인해야 할 현황이다.

- ZIP에는 `/assets/...`로 참조되는 이미지와 효과음 파일이 대부분 없다.
- HOME의 CHARACTERS 패널에는 1~21번 캐릭터와 14·15번 전투 이미지를 사용하는 흑백/컬러 Scan/Wipe 상태 머신이 적용되어 있다.
- HOME의 CHARACTERS 제목은 다른 패널 Hover 시에도 잘리지 않도록 패널 축소 폭과 반응형 글자 크기를 조정했다.
- HOME의 WEBTOON 패널에는 28번 이미지 0~6이, WEBTOON 감상 화면에는 27번 원고 0~28이 각각 연결되어 있다.
- CHARACTERS 상세 패널은 명시적인 ID 매핑을 통해 선택 캐릭터의 0번 외부 이미지를 표시한다.
- WORLD는 다섯 섬의 간격, 기후별 지형·건물·식생, 중앙섬의 도시 밀도, 하부 프레임·안정화 링·보조 엔진·에테르 흐름, 섬 선택 시 카메라 이동과 상세 패널까지 1차 구체화가 적용되어 있다.
- WORLD 렌더링에는 ACES 톤 매핑, 고해상도 그림자, 별빛·안개 대기, 엔진 입자, 고밀도 금속 재질이 적용되어 있으며 중앙섬–외곽섬 간 공중교 4개가 구현되어 있다.
- WORLD는 `America/Los_Angeles` 태평양 현지 시각을 30초마다 확인해 `night / dawn / day / sunset` 네 시간대로 나눈다.
- WORLD의 최신 시각 방향은 사실적인 풍경 재현보다 `전술 네트워크를 투영한 홀로그램 공중도시 지도`에 가깝다. 짙은 남색 바탕, 청록 발광 윤곽, 데이터 그리드·거리 링·섬별 투영 빔·회전 스캔 링·네트워크 연결선·화면 스캔라인을 핵심 언어로 사용하며, 기존 섬·다리·조직·시간·선택 데이터는 그대로 유지한다.
- 시간대에 따라 하늘색, 노출, 안개, 태양 위치·색온도·광량, 반구광, 별의 수, 구름색, 건물 창문 발광 강도와 그림자 방향을 함께 바꾼다. 네 시간대의 차이는 유지하되 전체 색역은 홀로그램을 읽기 쉬운 짙은 남색·청록 계열로 통일한다. 스카이 돔은 카메라 위치를 매 프레임 따라가므로 확대·축소 중 원형 경계가 화면에 드러나지 않는다.
- 낮에는 밝은 대기와 높은 태양, 새벽·저녁에는 낮은 태양과 노을, 밤에는 어두운 하늘·별빛·강한 창문 발광을 사용한다. 현재 태평양 시각과 상태를 WORLD HUD에 표시한다.
- 섬 아래 구름층은 홀로그램 정보가 가려지지 않는 낮은 밀도의 청색 데이터 안개처럼 표현한다. 구름은 섬보다 충분히 낮은 고도에만 배치하며, 원거리 안개도 섬 뒤쪽부터 시작해 장면 전체가 흰색으로 날아가지 않게 한다.
- 건물에는 층수에 비례한 다중 창문 행을 전후·측면에 배치하며, 태양 방향광과 PCF Soft Shadow로 건물·섬·시설물의 그림자를 만든다.
- 최종 렌더링은 256px 절차 지표·암반 텍스처, 장치 환경에 따라 1.25~2배 픽셀 밀도, 2048px 그림자 맵과 고해상도 섬·엔진 곡면을 사용한다. 카메라 조작 중 해상도를 낮추던 Adaptive DPR은 제거해 확대·회전 중에도 윤곽 해상도를 일정하게 유지한다.
- 금속 구조물에는 Lightformer 기반 환경 반사광을 적용하고, 시간대별 Unreal Bloom 후처리로 태양·창문·에테르·가로등 발광을 정돈한다.
- 구름 바다는 구형 메시 덩어리를 제거하고 고해상도 스프라이트형 볼류메트릭 구름만 사용해 각진 폴리곤 윤곽이 보이지 않게 한다. 구름 텍스처도 사이트 진입 시 선로딩한다.
- WORLD 후처리는 투명 구름에 오류를 만들고 비용이 큰 SSAO를 제거하고, 시간대별 채도·대비·밝기·비네팅 색 보정과 FXAA 윤곽 보정, 낮은 강도의 Bloom을 사용한다.
- 현재 Three/R3F 엔진은 웹 기반 고품질 렌더링에 충분하므로 엔진은 유지한다. 향후 품질의 가장 큰 상한은 엔진보다 전문 제작된 3D 모델·PBR 텍스처·환경 에셋의 유무다.
- 도시 밀도를 높이고 순환도로, 보도 경계, 가로등, 차량과 전후등, 착륙 패드, 옥상 HVAC, 다층 식생을 추가했다.
- 엔진에는 고밀도 냉각 핀·상태등, 세분화된 회전 링과 노즐 곡면을 적용했다.
- WORLD의 섬 표면은 절차적 지표 무늬와 암석층, 균열·층리, 지형 돌출부, 금속 옹벽과 가장자리 장갑판을 조합해 점토 같은 단일 재질 인상을 줄였다.
- WORLD의 도로에는 차선 표식과 서비스 노드가 있고, 건물에는 창 발광 띠·옥상 모듈·안테나가 있으며, 본부 건물에는 기단과 상부 설비가 추가되어 있다.
- 섬 하부에는 암반층 금속 이음새, 다중 지지대, 에테르 전력 도관과 상태등을 추가해 기계식 부유 구조의 밀도를 높였다.
- WORLD의 배치 히어로 행을 누르면 해당 캐릭터의 이미지와 등급·소속·펄스·성격·특징 상세 정보를 볼 수 있다.
- 중앙섬의 배치 히어로 목록은 `LADER`와 `PACTUM` 두 조직으로 분리해 표시한다.
- 정식 이름은 `윤세하`, `금하나`, `오하루`이며 현재 캐릭터 데이터와 일치한다.
- 현재 HISTORY·SOCIETY 등의 텍스트가 최신 합의와 정확히 일치하는지 추후 콘텐츠 검수가 필요하다.
- 현재 코드에는 타입 오류와 누락 의존성(`zustand`)이 있으나 Vite 프로덕션 빌드는 생성된다.

